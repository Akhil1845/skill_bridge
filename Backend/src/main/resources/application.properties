# ----------------------------------------------------
# Spring Boot Application Configuration
# ----------------------------------------------------
spring.application.name=SkillBridge

# ----------------------------------------------------
# PostgreSQL Connection (values injected via Render)
# ----------------------------------------------------
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d424dp63jp1c73ab0bug-a.oregon-postgres.render.com:5432/skillbridge_db_dd17}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:skillbridge_db_dd17_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:GiG2ATdFk4rSA4ggxqLIk4l3e4lrk11d}


# ----------------------------------------------------
# JPA / Hibernate Configuration
# ----------------------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ----------------------------------------------------
# Server Port
# Render provides PORT automatically
# Defaults to 8080 for local development
# ----------------------------------------------------
server.port=${PORT:8080}
